{
  "name": "interface-cap-academie",
  "version": "1.0.0",
  "description": "EdukaTchat - La plateforme numérique de l'enseignement ivoirien",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step required for static site'",
    "dev": "python3 -m http.server 8000 || python -m http.server 8000",
    "test": "echo 'No tests specified'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.3.1"
  },
  "devDependencies": {}
}
